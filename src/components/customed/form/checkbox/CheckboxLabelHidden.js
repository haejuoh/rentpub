import React from "react";
import SkrCheckbox from "../../../skr/SkrCheckbox";

const CheckboxLabelHidden = ({ ...rest }) => {
  return <SkrCheckbox {...rest} />;
};

export default CheckboxLabelHidden;
