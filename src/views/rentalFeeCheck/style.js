import { styled } from "@mui/styles";
const Table = styled("table")(({ theme }) => ({
  display: "table",
  width: "100%",
}));
const Thead = styled("thead")(({ theme }) => ({
  width: "100%",
}));
export { Wrap, Title };
